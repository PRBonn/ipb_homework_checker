base_node:
  root_folder: tests
  exercises:
  - name: Exercise 1      # This is a struct for exercise 1.
    language: cpp         # Type of the exercise.
    folder: excercise_1   # Name of the folder containing the exercise.
    output_type: string   # We expect a string as an output.
    tests:                # An exercise can have multiple tests.
      - name: Test 1
        expected_output: >
          This is a long test output that we expect to be produced by the code.
          We will compare the ouput to this EXACTLY.
      - name: Test 2
        input_args: Some string
        expected_output: >
          Some string output
